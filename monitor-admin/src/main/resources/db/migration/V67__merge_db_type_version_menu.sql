-- =============================================================
-- 合并「数据库类型 / 数据库版本」菜单入口
-- 保留 system_db_version 作为统一入口（页面内管理类型+版本）
-- =============================================================

-- 统一入口菜单：数据库类型与版本
UPDATE sys_menu
   SET sort = COALESCE(sort, 15)
 WHERE code = 'system_db_version';

