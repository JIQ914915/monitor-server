-- =============================================================
-- V131：清理遗留「报表」占位菜单
--   监控视图下的实例级「报表」菜单（component=monitor/report）为脚手架
--   阶段占位，页面从未实现；报告能力已由 V130「报告中心」统一承载，
--   原型中生成报告入口也已收敛到报告中心页面内，故删除该占位菜单。
-- =============================================================

DELETE FROM sys_menu WHERE code = 'report' AND component = 'monitor/report';
