-- =============================================================
-- 分组表调整：去除分组类型(type)
-- =============================================================
ALTER TABLE instance_group DROP COLUMN IF EXISTS type;
