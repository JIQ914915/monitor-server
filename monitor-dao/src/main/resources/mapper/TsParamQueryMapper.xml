<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lzzh.monitor.dao.mapper.TsParamQueryMapper">
    <select id="selectLatestNumeric" resultType="java.util.Map">
        SELECT DISTINCT ON (metric_code) metric_code, value
        FROM metric_data_1d
        WHERE instance_id = #{instanceId}
          AND metric_code IN
          <foreach collection="metricCodes" item="code" open="(" separator="," close=")">
              #{code}
          </foreach>
          AND collect_time > NOW() - INTERVAL '2 days'
        ORDER BY metric_code, collect_time DESC
    </select>

    <select id="selectLatestText" resultType="java.util.Map">
        SELECT DISTINCT ON (metric_code) metric_code, value_text
        FROM metric_text_data_1d
        WHERE instance_id = #{instanceId}
          AND metric_code IN
          <foreach collection="metricCodes" item="code" open="(" separator="," close=")">
              #{code}
          </foreach>
          AND collect_time > NOW() - INTERVAL '2 days'
        ORDER BY metric_code, collect_time DESC
    </select>
</mapper>
